import type { GameMeta } from '@funfair/shared';

export const whackMeta: GameMeta = {
  id: 'whack',
  name: 'Mole Mayhem',
  tagline: 'Swipe through the moles as they pop up. Chain them for combos, dodge the bombs.',
  minPlayers: 1,
  maxPlayers: 8,
};
